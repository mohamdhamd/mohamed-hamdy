# Build Prompt — Personal Portfolio Website (Mohamed Hamdy)

> برومبت تنفيذي كامل. كل حاجة هنا مقصودة — اللي مكتوب "إلزامي" مش قابل للتفاوض، واللي مكتوب "قرار مفتوح" في آخر الملف محتاج تأكيد قبل ما تبدأ.

---

## 1. البريف في سطرين

موقع شخصي (Personal Portfolio) لـ **محمد حمدي**، طالب محاسبة دولية بيذاكر CMA وبيبني أدوات ومواقع على الجنب.
الموقع **single-page** بتجربة تمرير واحدة، **ثنائي اللغة عربي/إنجليزي**، بهوية بصرية **فلكية** — مش "دارك مود بنجوم متطايرة"، ده هيبان AI من بعيد.

**الزاوية البصرية المقصودة:** علم الفلك العربي القديم. الإسطرلاب، خرائط النجوم على الرق، منازل القمر الـ 28، المخطوطات الفلكية. السماء هنا مرصودة ومحسوبة، مش مجرد خلفية زينة. ده اللي هيخلي الموقع مش شبه أي بورتفوليو تاني.

**الجمهور:** حد بيفتح الموقع عشان يشوف شغل محمد ويقرر يشتغل معاه.
**الوظيفة الأساسية للصفحة:** توصّل الزائر لقسم المشاريع. ده أهم جزء في الموقع.

---

## 2. الـ Stack

| الطبقة | التقنية |
|---|---|
| Frontend | React 18 + Vite |
| Styling | Tailwind CSS |
| الأنيميشن | CSS transitions + `requestAnimationFrame` للنجوم. متستخدمش مكتبة أنيميشن تقيلة |
| الأيقونات | `lucide-react` |
| الخطوط | Google Fonts (تفاصيلها تحت) |
| Backend | شوف قسم 8 |
| النشر | Vercel |

**قواعد ومعايير إلزامية (Production-First):**
- استخدام `localStorage` لحفظ تفضيل اللغة (`AR/EN`) وضمان ثبات التجربة بعد التحديث.
- الاعتماد الكامل على وسوم الـ HTML الدلالية (`<form>`, `<main>`, `<nav>`, `<article>`) مع معايير الوصولية (A11y & WCAG AA).
- الكود معلّق بالعربي المصري بشكل غني — كل دالة وكل مكون عليه تعليق تفصيلي يوضح هدفه ومنطقه.

---

## 3. نظام التصميم (Design Tokens)

### الألوان

```css
--void:        #05070F;  /* أسود مزرق عميق — خلفية السماء الأساسية */
--deep:        #0C1226;  /* كحلي أغمق — طبقات ثانية وخلفيات الكروت */
--ink:         #16203F;  /* حدود وفواصل خفيفة */
--brass:       #C9A227;  /* نحاس الإسطرلاب — لون التمييز الوحيد */
--moonlight:   #E6E2D3;  /* بياض الرق/ضوء القمر — لون النص الأساسي */
--dust:        #A4AFC4;  /* رمادي مزرق معزز لضمان تباين معايير WCAG AA — النص الثانوي */
```

**قواعد استخدام اللون (إلزامي):**
- **النحاس `--brass` هو لون التمييز الوحيد في الموقع.** ممنوع تضيف بنفسجي أو سماوي نيون أو تدرجات لونية كخلفيات زينة. التدرج الوحيد المسموح هو تدرج طبيعي خفيف من `--void` لـ `--deep` بيمثل عمق السماء.
- النص الأساسي `--moonlight` مش أبيض ناصع — عشان يدي إحساس الرق مش إحساس الشاشة.
- ممنوع نفس الـ `box-shadow` الرمادي تحت كل كارت.

### الخطوط

خط واحد بيغطي العربي والإنجليزي، وخط عرض واحد للحظات الكبيرة:

```
Body / UI  →  IBM Plex Sans Arabic   (400, 500, 600)
Display    →  Aref Ruqaa             (للعناوين العربية الكبيرة — إحساس المخطوط)
Mono       →  IBM Plex Mono          (لتأثير الكتابة على اسمك + التواريخ الهجرية)
```

**سلم الأحجام:**
```
اسم الـ Hero   clamp(2.75rem, 9vw, 6rem)   / letter-spacing: 0.08em
عنوان القسم    clamp(1.75rem, 4vw, 2.75rem)
نص عادي        1.0625rem / line-height 1.75  (العربي محتاج line-height أعلى)
نص ثانوي       0.9375rem
```

**ممنوع (دي علامات التصميم المولّد):**
- ❌ كلمة واحدة ملوّنة جوه العنوان
- ❌ لابلات بـ ALL CAPS متباعدة فوق كل عنوان
- ❌ سهم `→` ملزوق في آخر كل زرار
- ❌ ترقيم `01 / 02 / 03` إلا لو المحتوى فعلاً تسلسل

### اللي هيتصرف فيه الميزانية البصرية

**القمر هو النجم.** هو العنصر الجريء الوحيد في الصفحة. كل حاجة حواليه تفضل هادية ومنضبطة. لو حسّيت إن فيه عنصر تاني بيشد العين زيه — شيله.

---

## 4. الـ Layout — الخريطة الكاملة

```
┌───────────────────────────────────────────────┐
│  [محمد حمدي]          عني · مشاريعي · شهاداتي │  ← nav ثابت
│                                    تواصل  AR/EN│
├───────────────────────────────────────────────┤
│                                               │
│            م ح م د   ح م د ي                  │  ← 1. HERO
│         (بيتكتب حرف حرف عند التحميل)          │     شاشة كاملة
│              سطر تعريفي واحد                   │
│                     ↓                          │
├───────────────────────────────────────────────┤
│  ┌─────────────┐  │  ┌──────────────────┐     │  ← 2. عني
│  │ نص تعريفي   │  │  │      ◐           │     │     مقسوم نصين
│  │ عني بإيجاز  │  │  │   القمر الهجري   │     │
│  │             │  │  │  ١٤ ربيع الأول   │     │
│  └─────────────┘  │  └──────────────────┘     │
├───────────────────────────────────────────────┤
│              مشاريعي                          │  ← 3. المشاريع
│   [فلتر: الكل · ويب · أدوات · محاسبة]        │     ★ الأهم
│   ┌────────┐ ┌────────┐ ┌────────┐           │
│   │ مشروع  │ │ مشروع  │ │ مشروع  │           │
│   └────────┘ └────────┘ └────────┘           │
├───────────────────────────────────────────────┤
│              شهاداتي                          │  ← 4. الشهادات
│   ▸ شهادة — الجهة — التاريخ                  │     تحت خالص
│   ▸ شهادة — الجهة — التاريخ                  │
├───────────────────────────────────────────────┤
│   تواصل معي   ·  إيميل · لينكدإن · جيتهاب    │  ← 5. تواصل/فوتر
└───────────────────────────────────────────────┘
```

**Nav:** ثابت فوق، خلفية شفافة بـ backdrop-blur. بيخفّ ويصغر بعد أول scroll. على الموبايل بيتحول لـ drawer جانبي. الـ nav بيعلّم القسم الحالي (scroll spy).

---

## 5. الـ Hero — لحظة الدخول

دي الحركة المنظّمة الوحيدة في الموقع كله. تسلسل التحميل:

1. **0ms** — شاشة سودا. حقل النجوم بيبدأ يظهر بـ fade على مدى 1200ms.
2. **400ms** — الاسم يبدأ يتكتب حرف حرف. سرعة **70ms/حرف** مع عشوائية ±20ms عشان الإيقاع يبان بشري مش آلي.
3. **مؤشر الكتابة** خط عمودي رفيع بلون `--brass`، بيرمش كل 530ms، وبيختفي بعد آخر حرف بـ 800ms.
4. **بعد اكتمال الاسم بـ 300ms** — السطر التعريفي يظهر fade.
5. **بعد السطر بـ 400ms** — سهم النزول يظهر.

**تفاصيل إلزامية للـ typewriter:**
- الاسم بيتكتب حسب اللغة الحالية: "محمد حمدي" في العربي، "Mohamed Hamdy" في الإنجليزي.
- **احجز مكان الاسم مقدمًا** (`min-height` ثابت) عشان الصفحة متنطّش أثناء الكتابة.
- التأثير بيشتغل عند أول تحميل للموقع (أول دخول)، وعند تبديل اللغة يتغير النص فوراً بنعومة دون إعادة الكتابة لتفادي إرهاق الزائر (UX Friction).
- `aria-label` على العنصر فيه الاسم كامل، والحروف اللي بتتكتب `aria-hidden` — عشان قارئ الشاشة يقرا الاسم مرة واحدة صح.
- **`prefers-reduced-motion`**: الاسم يظهر كامل فورًا من غير كتابة.

**حقل النجوم:**
- `<canvas>` مش DOM elements. عدد النجوم = `min(180, عرض الشاشة / 8)`.
- كل نجمة ليها لمعان خاص بيها بيتذبذب ببطء (فترة عشوائية 3–7 ثواني).
- **parallax خفيف جدًا** مع الماوس — إزاحة أقصاها 12px. على الموبايل الحركة دي مقفولة.
- الـ animation loop **بيقف** لما التاب يبقى مخفي (`visibilitychange`) وبيقف مع `prefers-reduced-motion`.
- ممنوع نجوم بتتحرك أو بتقع أو خطوط بتوصل بينها. النجوم ثابتة وبتلمع بس.

---

## 6. قسم "عني" — والقمر

### النص (الجهة الأولى)

فقرتين قصيرتين بالعربي والإنجليزي. مكتوبة بصيغة المتكلم، من غير مبالغة ومن غير كلام تسويقي فاضي. لو المحتوى النهائي لسه مش جاهز، اكتب placeholder حقيقي معقول (طالب محاسبة دولية، CMA، بيبني أدوات ويب) مش "Lorem ipsum".

### القمر الهجري (الجهة التانية) — أهم جزء تقني في الواجهة

القمر **مربوط بالتاريخ الهجري الحقيقي**. اللي بيزور الموقع النهاردة بيشوف شكل القمر الحقيقي في السما دلوقتي.

**حساب التاريخ الهجري:**
```js
// من غير أي مكتبة خارجية — المتصفح بيعمل ده أصلاً
const fmt = new Intl.DateTimeFormat('ar-SA-u-ca-islamic-umalqura', {
  day: 'numeric', month: 'long', year: 'numeric'
});
// النتيجة: "١٤ ربيع الأول ١٤٤٨ هـ"
```

**حساب طور القمر (أدق من مجرد يوم الشهر):**
```js
const SYNODIC = 29.530588853;              // طول الشهر القمري بالأيام
const EPOCH   = Date.UTC(2000, 0, 6, 18, 14); // محاق مرجعي معروف
const days    = (Date.now() - EPOCH) / 86400000;
const phase   = ((days % SYNODIC) + SYNODIC) % SYNODIC / SYNODIC; // 0..1
// phase = 0 محاق، 0.25 تربيع أول، 0.5 بدر، 0.75 تربيع أخير
const illum   = (1 - Math.cos(2 * Math.PI * phase)) / 2; // نسبة الإضاءة
```

**الرسم (SVG، مش صورة):**
- قرص دائري نصف قطره `R` بلون `--deep` = الجزء المظلم.
- الجزء المضيء مبني من **نصف قرص + قطع ناقص (ellipse)** نصف محوره الأصغر `rx = R * |cos(2π·phase)|`.
- **اتجاه الهلال:** لما `phase < 0.5` (متزايد) الإضاءة على **اليمين**. لما `phase > 0.5` (متناقص) الإضاءة على **الشمال**. ده الصح فلكيًا في نصف الكرة الشمالي — ومصر منه.
- سطح القمر عليه فوهات (craters) خفيفة جدًا بـ opacity منخفض، بتتقص مع الجزء المظلم.
- هالة ضوء ناعمة حوالين القمر — قوتها **متناسبة مع نسبة الإضاءة**، يعني البدر بيضوّي والمحاق مش بيضوّي.

**تحت القمر:**
```
١٤ ربيع الأول ١٤٤٨ هـ
بدر  ·  ٩٨٪ إضاءة
```
أسماء الأطوار بالعربي: `محاق · هلال متزايد · تربيع أول · أحدب متزايد · بدر · أحدب متناقص · تربيع أخير · هلال متناقص`

**التفاعل:** سلايدر أفقي رفيع تحت القمر يخلّي الزائر يلف على أيام الشهر الهجري ويشوف الشكل بيتغير. لما يسيبه، يرجع لليوم الحالي بحركة ناعمة. ده تفاعل بيستحق الأنيميشن لأنه رد فعل على فعل المستخدم.

**Fallback:** لو `Intl` مرجّعش التقويم الإسلامي (متصفح قديم)، اعرض القمر من حساب الطور بس من غير التاريخ الهجري النصي.

---

## 7. قسم المشاريع — الأهم

### العرض

- **Grid:** 3 أعمدة ديسكتوب، 2 تابلت، 1 موبايل.
- كل كارت فيه: صورة الغلاف، العنوان، وصف سطرين، تاجات التقنيات، ورابط/رابطين.
- **الفلاتر:** أزرار فوق الشبكة (الكل / ويب / أدوات / محاسبة) — الفلاتر بتتولد من التصنيفات الموجودة فعلًا في البيانات، مش مكتوبة ثابتة في الكود.
- **المشاريع المميزة (featured)** بتاخد كارت بعرض مضاعف في أول الشبكة.
- لما تدوس على كارت → **modal** فيه تفاصيل المشروع كاملة + معرض صور + الروابط.
- **الحالة الفاضية:** لو مفيش مشاريع، اعرض دعوة واضحة للفعل مش رسالة اعتذار.

### نموذج البيانات

```ts
Project {
  id:            string
  title_ar:      string
  title_en:      string
  summary_ar:    string      // سطرين للكارت
  summary_en:    string
  body_ar:       string      // الوصف الكامل للـ modal
  body_en:       string
  cover:         string      // رابط صورة الغلاف
  gallery:       string[]
  tech:          string[]    // ["React", "Tailwind"]
  category:      string
  live_url:      string | null
  repo_url:      string | null
  featured:      boolean
  order:         number      // للترتيب اليدوي
  status:        "published" | "draft"
  created_at:    string
}

Certificate {
  id, title_ar, title_en, issuer, issue_date,
  credential_url, image, order
}
```

---

## 8. الباك إند ولوحة التحكم

الهدف: **تضيف مشاريعك بنفسك من غير ما تحتاج تعدّل كود.**

### المسار الافتراضي: Supabase

```
جدولين:  projects  |  certificates
Storage bucket:  project-images
الصلاحيات (RLS):  قراءة عامة للمنشور فقط · كتابة للمستخدم المسجّل فقط
المصادقة:  Supabase Auth — إيميل واحد (بتاعك)
```

**لوحة التحكم على `/admin`:**
- صفحة دخول بسيطة (إيميل + باسورد).
- جدول بكل المشاريع مع أزرار: تعديل، مسح، تبديل منشور/مسودة.
- فورم إضافة/تعديل فيه **كل الحقول بالعربي والإنجليزي جنب بعض**.
- **رفع الصور بالسحب والإفلات** مع معاينة فورية.
- **إعادة الترتيب بالسحب** (drag to reorder) وبيتحفظ في `order`.
- تأكيد قبل أي عملية مسح.
- نفس المنطق للشهادات في تاب تاني.
- **لوحة التحكم دي منفصلة عن الموقع** ومش موجودة في الـ nav العام.

### البديل (لو عايزه Artifact هنا في الشات أول)

نفس الواجهة بالظبط، بس بدل Supabase استخدم نظام التخزين المتاح في الـ artifact لحفظ المشاريع بشكل دائم — بحيث تجرّب الشكل وتضيف مشاريع فعلًا قبل ما ننقله لموقع مستقل.

---

## 9. الثنائية اللغوية (AR / EN)

- **زرار تبديل** في الـ nav (`ع / EN`) واضح ومقروء.
- التبديل بيغيّر:
  - `dir` على `<html>` بين `rtl` و `ltr`
  - `lang` على `<html>`
  - كل النصوص بما فيها الاسم في الـ hero وأسماء الأطوار والتواريخ
- **الاتجاه الافتراضي: عربي/RTL.**
- استخدم Tailwind logical properties (`ps-`, `pe-`, `ms-`, `me-`, `text-start`, `text-end`) **مش** `pl-`/`pr-`. ده يخلي التخطيط ينعكس لوحده.
- الأرقام في العربي تتعرض بالأرقام العربية-الهندية (`١٢٣`)، وفي الإنجليزي بالأرقام اللاتينية.
- الاختيار بيتحفظ ويفضل بعد الـ reload.
- **القمر مش بينعكس مع اللغة** — اتجاه الهلال فلكي مش لغوي.

---

## 10. الموبايل والجودة

**الموبايل مش فكرة لاحقة — الموقع لازم يبقى مظبوط عليه بالكامل:**
- Breakpoints: `sm 640 · md 768 · lg 1024 · xl 1280`
- قسم "عني" بيتكوّم عموديًا على الموبايل — **القمر فوق والنص تحت**.
- عدد النجوم بيقل وparallax الماوس بيتقفل.
- كل هدف لمس مساحته **44×44px** على الأقل.
- الـ nav بيبقى drawer بحركة واحدة نضيفة.
- `font-size` ما يقلّش عن 16px في أي input (عشان iOS ميزوّمش).

**أرضية الجودة (إلزامية):**
- `prefers-reduced-motion` محترم في كل حركة — النجوم، الكتابة، الانتقالات.
- focus ظاهر بوضوح على كل عنصر تفاعلي (حلقة بلون `--brass`).
- التباين اللوني يعدّي WCAG AA.
- الصور عليها `alt` حقيقي + `loading="lazy"` + أبعاد محجوزة (مفيش layout shift).
- HTML دلالي: `<nav> <main> <section> <article> <footer>`.
- Meta tags كاملة + Open Graph + `<title>` وصفي.

---

## 11. معايير القبول — شيك ليست قبل التسليم

- [ ] الـ reload بيشغّل تأثير الكتابة من الأول في كل مرة
- [ ] القمر بيعرض الطور الصحيح لتاريخ النهاردة الهجري
- [ ] الهلال المتزايد مضيء من اليمين والمتناقص من الشمال
- [ ] السلايدر بيغيّر شكل القمر ويرجع لليوم الحالي
- [ ] تبديل اللغة بيقلب الاتجاه والنصوص من غير ما يكسر أي تخطيط
- [ ] قسم الشهادات آخر قسم قبل الفوتر
- [ ] لوحة التحكم بتضيف وتعدّل وتمسح مشروع والتغيير بيبان فورًا
- [ ] رفع صورة شغال وبيتحفظ
- [ ] الموقع مظبوط على عرض 375px من غير scroll أفقي
- [ ] `prefers-reduced-motion` بيوقّف كل الحركة
- [ ] الـ canvas بيقف لما التاب يتقفل
- [ ] الكود كله معلّق بالعربي المصري

---

## 12. القرارات المعمارية المعتمدة بعد المراجعة النقدية

1. **الخط العربي للعناوين:** اعتماد `Aref Ruqaa` للعنوان الرئيسي (Hero) فقط كـ Statement Piece فلكية فريدة، واستخدام `IBM Plex Sans Arabic` لباقي العناوين والواجهة بالكامل لتحقيق أعلى مقروئية وهندسة بصرية متناسقة.

2. **نقطة البداية ومعمارية البيانات:** البدء فوراً بمشروع Vite + React + TypeScript مستقل في المستودع المحلي، مع هيكلة بيانات المشاريع والشهادات كـ TypeScript Data Models قوية وسريعة، جاهزة للربط المستقبلي بـ Supabase عند الحاجة دون تعطيل الإطلاق الأولي.

3. **الذكاء الاصطناعي (AI):** تجنب إقحام Chatbot عشوائي مشتت للعين؛ والتركيز على إبراز الذكاء الرياضي والتحليلي في أدوات النمذجة المالية ومحاكي أطوار القمر.

4. **المحتوى الحقيقي:** بناء نموذج محتوى احترافي يربط تخصص المحاسبة والـ CMA بالقدرات البرمجية، متضمناً مشاريع واقعية في التحليل المالي وأتمتة القيود.

5. **تصنيفات المشاريع المعتمدة:**
   - **حلول مالية ومحاسبية (Finance & Accounting):** نماذج CVP، التدفقات النقدية، وتحليل الانحرافات.
   - **أنظمة وأتمتة (Systems & Automation):** أتمتة قيود اليومية، مطابقة دفاتر الأستاذ، وفحص الفواتير.
   - **أدوات وتطبيقات ويب (Web Apps & Tools):** محرك الإسطرلاب، حاسبة الإهلاك، والأدوات التفاعلية.
